{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Overlay window: receive events; dialog is invoked from Rust.",
  "windows": ["overlay"],
  "permissions": ["core:default", "core:event:default"]
}
