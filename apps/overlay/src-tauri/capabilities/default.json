{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Overlay + settings windows: receive events and invoke app commands.",
  "windows": ["overlay", "settings"],
  "permissions": ["core:default", "core:event:default"]
}
